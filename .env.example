# MongoDB connection string
MONGOOSE_URL="mongodb://username:password@host:port?authSource=admin"

# Discord webhooks for logging
WEBHOOK_TOKEN="Discord webhook token"
WEBHOOK_ADDBOT="Discord webhook URL for bot submissions"
WEBHOOK_BOT="Discord webhook URL for bot approvals"
WEBHOOK_LOGIN="Discord webhook URL for login logs"
WEBHOOK_RAW="Discord webhook URL for raw bot data"

# Server port
PORT=80

# API authentication secret
AUTH="API authentication secret key"

# Discord OAuth2 credentials
CLIENT_TOKEN="Discord bot token"
CLIENT_ID="Discord application client ID"
CLIENT_SECRET="Discord OAuth2 client secret"

# OAuth2 redirect URIs
REDIRECT_URI="API callback URL (e.g., https://api.yourdomain.com/api/auth/callback)"
REDIRECT_AUTH="Frontend redirect URL (e.g., https://yourdomain.com)"

# Session cookie secret
COOKIE_SECRET="Random secret for signing session cookies"

# Discord OAuth2 scopes
SCOPES='["identify", "guilds"]'

# JSON Web Token secret
JWT_SECRET="Secret key for signing JWT tokens"